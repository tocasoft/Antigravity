# Habilitar el motor de reescritura
Options +FollowSymLinks
RewriteEngine On

# -------------------------------------------------------------------------
# EJEMPLOS DE ENMASCARAMIENTO DE URL (URL MASKING)
# -------------------------------------------------------------------------

# Si quieres que el dominio "test.geniusdevelops.com" muestre "index.html"
# (Esto es útil si el dominio apunta a una carpeta y quieres forzar el archivo)
RewriteCond %{HTTP_HOST} ^test\.geniusdevelops\.com$ [NC]
RewriteRule ^$ index.html [L]

# -------------------------------------------------------------------------
# CONFIGURACIÓN COMÚN: ELIMINAR EXTENSIÓN .HTML
# -------------------------------------------------------------------------
# Si el archivo solicitado no es un directorio
RewriteCond %{REQUEST_FILENAME} !-d
# Y si el archivo con extensión .html existe
RewriteCond %{REQUEST_FILENAME}.html -f
# Entonces reescribe la solicitud internamente a .html
RewriteRule ^(.*)$ $1.html [L]
